# 股票查詢 API 功能實現總結

**實現日期**: 2025-12-16  
**狀態**: ✅ 已完成

---

## ✅ 已實現的功能

### 1. 股票查詢服務（stockService.ts）

#### 功能描述
- **問題**: 搜索功能只能搜索本地模擬數據，無法查詢實際股票價格
- **解決方案**: 實現股票查詢服務，支持本地查找和 API 查詢

#### 實現細節

**查詢流程**:
1. **本地查找優先**: 先從本地 `mockStocks` 數據中查找
2. **API 查詢**: 如果本地沒有找到，且查詢是 4 位數股票代碼，則調用 API
3. **Fallback**: 如果 API 查詢失敗，使用模擬數據作為 fallback

**API 集成**:
- 支持多個 API 端點（可擴展）
- 自動處理 API 失敗情況
- 使用 debounce 減少 API 請求頻率

**模擬數據支持**:
- 為常見股票代碼提供模擬數據
- 如果 API 不可用，自動使用模擬數據

---

### 2. 搜索功能增強

#### 功能描述
- 當用戶輸入 4 位數股票代碼時，自動查詢股票價格
- 查詢結果自動添加到股票列表中

#### 實現細節

**查詢觸發條件**:
- 輸入是 4 位數字（台灣股票代碼格式）
- 本地數據中沒有匹配的股票
- 使用 800ms debounce 避免頻繁請求

**用戶界面**:
- 載入狀態顯示（旋轉圖標）
- 錯誤訊息顯示（紅色提示）
- 查詢到的股票自動添加到列表

---

### 3. 載入狀態和錯誤處理

#### 載入狀態
- 搜索框顯示載入圖標（Loader2 旋轉動畫）
- 搜索框在載入時禁用輸入
- 視覺反饋：半透明效果

#### 錯誤處理
- API 請求失敗時顯示錯誤訊息
- 找不到股票時顯示友好提示
- 錯誤訊息顯示在搜索框下方（紅色背景）

---

## 📊 技術實現

### 服務結構

```typescript
src/services/stockService.ts
├── queryStock()              // 主查詢函數
├── findStockInLocalData()    // 本地查找
├── queryStockFromAPI()       // API 查詢
└── generateMockStockData()   // 模擬數據生成（fallback）
```

### 數據流程

```
用戶輸入股票代碼
    ↓
檢查是否為 4 位數
    ↓
本地數據查找
    ├─ 找到 → 返回結果
    └─ 未找到 → API 查詢
         ├─ 成功 → 添加到列表
         └─ 失敗 → 使用模擬數據（fallback）
```

### API 查詢邏輯

**支持的 API**:
1. 台股投資追蹤工具 API（優先）
2. FinMind API（備選）
3. 自定義後端 API（可擴展）

**CORS 處理**:
- 當前實現嘗試直接調用 API
- 如果遇到 CORS 問題，需要：
  - 使用後端代理
  - 或配置 API 服務器支持 CORS
  - 或使用 CORS 代理服務

---

## 🔧 使用方式

### 基本使用

用戶在搜索框中輸入股票代碼（如 "2317"）：
1. 系統先檢查本地數據
2. 如果沒有，自動查詢 API
3. 查詢結果顯示在股票列表中
4. 可以收藏、查看價格等信息

### 支持的查詢格式

- **股票代碼**: 4 位數字（如 "2330"、"2317"）
- **股票名稱**: 中文名稱（如 "台積電"、"鴻海"）

---

## ⚠️ 注意事項

### API 限制

1. **CORS 限制**:
   - 大多數公開 API 可能有 CORS 限制
   - 建議使用後端代理或配置 CORS

2. **API 配額**:
   - 免費 API 通常有請求頻率限制
   - 已實現 debounce（800ms）減少請求

3. **數據準確性**:
   - 當前使用模擬數據作為 fallback
   - 實際應用中應確保 API 數據的準確性

### 建議的改進

1. **後端代理**:
   - 創建後端 API 代理，避免 CORS 問題
   - 可以添加認證和請求限制

2. **緩存機制**:
   - 緩存查詢結果，減少 API 請求
   - 使用 localStorage 或 IndexedDB

3. **錯誤重試**:
   - API 失敗時自動重試
   - 指數退避策略

4. **多數據源**:
   - 集成多個股票 API
   - 實現數據源切換和備援

---

## 🚀 未來擴展

### API 集成選項

1. **FinMind API**:
   - 提供台灣股票數據
   - 需要註冊獲取 API Key
   - 文檔: https://finmind.github.io/

2. **台股投資追蹤工具 API**:
   - 免費提供即時股價
   - 支持個股、ETF 查詢
   - 文檔: https://www.taiwanstock.online/api-docs.html

3. **自建後端**:
   - 使用 Node.js/Python 創建後端 API
   - 處理 API 認證和數據轉換
   - 避免前端 CORS 問題

### 功能擴展

1. **即時價格更新**:
   - WebSocket 連接
   - 定期輪詢更新價格

2. **歷史價格查詢**:
   - 查詢股票歷史價格
   - 顯示價格趨勢圖

3. **批量查詢**:
   - 一次查詢多支股票
   - 批量更新價格

---

## 📝 代碼示例

### 查詢股票

```typescript
import { queryStock } from './services/stockService';

// 查詢股票
const result = await queryStock('2330', localStocks, true);

if (result.found && result.stock) {
  console.log('找到股票:', result.stock);
  // 使用 result.stock
} else {
  console.error('未找到股票:', result.error);
}
```

### 批量查詢

```typescript
import { queryStocks } from './services/stockService';

const results = await queryStocks(['2330', '2317'], localStocks);
results.forEach((result) => {
  if (result.found) {
    console.log('找到:', result.stock);
  }
});
```

---

## ✅ 測試建議

### 功能測試

1. **本地數據查找**:
   - 輸入已有股票的代碼（如 "2330"）
   - 驗證立即顯示結果

2. **API 查詢**:
   - 輸入不存在的股票代碼（如 "2317"）
   - 驗證載入狀態顯示
   - 驗證查詢結果添加到列表

3. **錯誤處理**:
   - 輸入無效代碼（如 "9999"）
   - 驗證錯誤訊息顯示

4. **Debounce**:
   - 快速輸入多個字符
   - 驗證只在停止輸入後才發起請求

---

## 📋 實現細節補充

### Debounce 機制

為了避免頻繁的 API 請求，實現了 800ms 的 debounce 機制：
- 用戶輸入時，清除之前的 timeout
- 停止輸入 800ms 後才發起 API 請求
- 組件卸載時自動清理 timeout

### 查詢優先級

1. **本地數據優先**: 先從 `mockStocks` 查找
2. **API 查詢**: 僅當輸入是 4 位數代碼且本地沒有時
3. **Fallback**: API 失敗時使用模擬數據

### 支持的股票代碼

當前模擬數據支持以下常見台灣股票：
- 2317: 鴻海
- 2454: 聯發科
- 2308: 台達電
- 2412: 中華電
- 3008: 大立光

其他 4 位數代碼也會嘗試查詢 API，如果 API 不可用則生成模擬數據。

---

**實現完成**: 股票查詢功能已實現並通過構建測試 ✅









