# Google Apps Script 授權步驟詳解

## 🚨 您遇到的錯誤

```
Exception: Unexpected error while getting the method or property openById on object SpreadsheetApp.
```

這表示 Google Apps Script **需要授權**才能訪問 Google Sheets。

---

## ✅ 解決步驟

### 步驟 1: 觸發授權提示

當您運行 `initializeSheet` 或任何需要訪問 Sheet 的函數時，應該會出現授權提示。如果沒有出現，請：

1. **在 Google Apps Script 編輯器中**
2. **再次點擊「執行」按鈕**運行 `initializeSheet`
3. **等待授權提示出現**

---

### 步驟 2: 完成授權流程（關鍵步驟）

當出現授權提示時：

1. **點擊「檢閱權限」**按鈕
2. **選擇您的 Google 帳號**
3. **會看到警告訊息**（這是正常的）：
   - "Google 尚未驗證此應用程式"
   - 點擊「**進階**」
4. **點擊「前往 [專案名稱]（不安全）」**
   - 這對於您自己的腳本是正常的
5. **點擊「允許」** ← **這是最關鍵的步驟！**

---

### 步驟 3: 重新運行函數

完成授權後：

1. **再次運行 `initializeSheet`**函數
2. **查看執行記錄**：
   - ✅ **成功**：應該看到「工作表已成功創建」或「工作表已存在」
   - ❌ **如果仍然失敗**：檢查下面的故障排除

---

## 🔍 如果沒有出現授權提示

### 檢查項目：

1. **確認 Sheet ID 是否正確**：
   - 打開您的 Google Sheet
   - 從 URL 中複製 Sheet ID
   - 更新 `Code.gs` 中的 `SHEET_ID` 常量

2. **手動觸發授權**：
   - 前往：https://myaccount.google.com/permissions
   - 找到您的 Apps Script 專案
   - 撤銷權限（如果存在）
   - 重新運行 `initializeSheet`

3. **檢查部署設置**：
   - 點擊「部署」→「管理部署」
   - 確認「執行身分」設置為「我」

---

## 🎯 快速驗證授權是否成功

完成授權後，再次運行 `initializeSheet`：

**成功的執行記錄應該顯示**：
```
資訊 工作表已成功創建
```
或
```
資訊 工作表已存在
```

**如果仍然失敗**，請檢查：
1. Sheet ID 是否正確
2. Sheet 是否存在
3. 您是否是 Sheet 的所有者

---

## 📋 完整流程檢查清單

- [ ] 已運行 `initializeSheet` 函數
- [ ] 已看到授權提示
- [ ] 已點擊「檢閱權限」
- [ ] 已選擇 Google 帳號
- [ ] 已點擊「進階」→「前往 [專案名稱]（不安全）」
- [ ] **已點擊「允許」** ← 最重要！
- [ ] 已重新運行 `initializeSheet`
- [ ] 執行記錄顯示成功訊息

---

## ⚠️ 常見問題

### 問題 1: 授權提示沒有出現

**可能原因**：
- 之前已經拒絕過授權
- 瀏覽器阻止了彈窗

**解決方案**：
1. 清除瀏覽器快取
2. 前往 https://myaccount.google.com/permissions
3. 撤銷之前的權限
4. 重新運行函數

---

### 問題 2: 點擊「允許」後仍然失敗

**檢查**：
1. ✅ Sheet ID 是否正確
2. ✅ Sheet 是否存在
3. ✅ 您是否是 Sheet 的所有者
4. ✅ 「執行身分」是否設置為「我」

---

### 問題 3: 執行記錄沒有顯示詳細訊息

**解決方案**：
1. 確保代碼中包含 `Logger.log()` 語句
2. 檢查執行記錄是否已展開
3. 查看是否有錯誤訊息（紅色文字）

---

**最後更新**: 2025-12-16
