# 解決 Google Apps Script 302 重定向問題

## 🚨 問題診斷

從瀏覽器開發者工具的 Network 標籤可以看到：

```
Status Code: 302 Found
Request URL: https://script.google.com/macros/s/YOUR_ID/exec
Request Method: GET
```

**這是什麼意思？**

`302 Found` 表示服務器正在**重定向**請求到另一個 URL，而不是直接返回數據。

---

## ⚠️ 為什麼會出現 302？

### 原因：部署權限未設置為「任何人」

當 Google Apps Script Web App 的部署權限不是「任何人」時：

1. ❌ **未認證的請求**會被重定向到 Google 登入頁面
2. ❌ **跨域請求**無法完成（因為被重定向）
3. ❌ **CORS headers** 不會被添加（因為請求沒有到達實際的腳本）

### 預期行為（正確設置）

當部署權限設置為「任何人」時：

- ✅ **直接返回 JSON 響應**（200 OK）
- ✅ **沒有重定向**
- ✅ **自動添加 CORS headers**

---

## ✅ 解決步驟

### 步驟 1: 檢查當前部署權限

1. **前往 Google Apps Script 編輯器**
   - https://script.google.com
   - 打開您的專案

2. **點擊「部署」→「管理部署」**

3. **檢查「具有存取權的使用者」設置**
   - 如果顯示「僅限我自己」或您的 Google 帳號 → ❌ **需要修改**
   - 如果顯示「任何人」→ ✅ **權限正確**（但可能需要重新部署）

---

### 步驟 2: 修改部署權限

1. **點擊部署右側的「編輯」圖標** ✏️

2. **在「具有存取權的使用者」選項中**：
   - ❌ **不要選擇**「僅限我自己」
   - ✅ **必須選擇**「任何人」

3. **確認「執行身分」設置**：
   - ✅ 選擇「我（your-email@gmail.com）」

---

### 步驟 3: 重新部署

1. **點擊「部署」按鈕**
2. **等待部署完成**（通常幾秒鐘）
3. **確認部署 URL**（應該保持不變）

---

### 步驟 4: 清除瀏覽器快取並重新測試

1. **清除瀏覽器快取**
   - Chrome/Edge: `Ctrl+Shift+Delete` (Windows) 或 `Cmd+Shift+Delete` (Mac)
   - 選擇「快取的圖片和檔案」
   - 點擊「清除資料」

2. **或者使用無痕模式測試**
   - `Ctrl+Shift+N` (Windows) 或 `Cmd+Shift+N` (Mac)

3. **重新訪問您的 Apps Script URL**
   ```
   https://script.google.com/macros/s/YOUR_ID/exec
   ```

---

## 🔍 驗證修復

### 方法 1: 直接訪問 URL（瀏覽器）

**預期結果（成功）**：
```json
{
  "success": true,
  "message": "Google Apps Script Web App 運行正常",
  "timestamp": "2025-12-16T..."
}
```

**如果仍然看到 302**：
- ❌ 部署權限可能沒有正確保存
- ❌ 瀏覽器可能使用了快取的舊響應
- ❌ 需要再次檢查部署設置

---

### 方法 2: 檢查 Network Headers

1. **打開瀏覽器開發者工具**（F12）
2. **切換到 Network 標籤**
3. **清除記錄**（點擊清除圖標）
4. **訪問您的 Apps Script URL**

**預期結果**：
- ✅ **Status Code**: `200 OK`（不再是 302）
- ✅ **Response Headers**: 包含 `Access-Control-Allow-Origin: *`
- ✅ **Response**: 直接顯示 JSON 數據（沒有重定向）

**如果仍然是 302**：
- 檢查 Response Headers 中的 `Location` header
- 這會告訴您請求被重定向到哪裡
- 通常會被重定向到 Google 登入頁面

---

## 📊 狀態碼對比

| 狀態碼 | 含義 | 原因 | 解決方案 |
|--------|------|------|----------|
| ✅ **200 OK** | 請求成功 | 部署權限設置為「任何人」 | 無需操作 |
| ❌ **302 Found** | 請求被重定向 | 部署權限不是「任何人」 | 設置權限為「任何人」並重新部署 |
| ❌ **403 Forbidden** | 禁止訪問 | 部署權限不是「任何人」 | 設置權限為「任何人」並重新部署 |
| ❌ **404 Not Found** | 找不到資源 | URL 錯誤或部署不存在 | 檢查 URL 是否正確 |

---

## 🔄 常見問題

### Q1: 我已經設置為「任何人」，為什麼還是 302？

**可能原因**：
1. **設置未保存**：檢查是否點擊了「部署」按鈕保存更改
2. **瀏覽器快取**：清除瀏覽器快取或使用無痕模式
3. **錯誤的 URL**：確認使用的是正確的部署 URL（應該以 `/exec` 結尾，不是 `/dev`）

**解決方案**：
1. 重新進入部署管理
2. 確認「具有存取權的使用者」確實顯示為「任何人」
3. 如果已更改，重新部署
4. 清除瀏覽器快取並重新測試

---

### Q2: `/dev` 和 `/exec` 有什麼區別？

| URL 後綴 | 用途 | 權限要求 |
|---------|------|---------|
| `/dev` | 開發版本（最新代碼） | 通常需要登入 |
| `/exec` | 生產版本（已部署的版本） | 根據部署權限 |

**重要**：
- ✅ **生產環境必須使用 `/exec`**
- ❌ `/dev` 通常不支持公開訪問（會返回 302 或 403）

---

### Q3: 設置為「任何人」是否安全？

**安全性說明**：

1. ✅ **執行身分仍然可以控制**：
   - 設置「執行身分」為「我」
   - 腳本以您的身份執行（有權限訪問您的 Google Sheets）

2. ✅ **訪問控制**：
   - 只有知道 URL 的人才能訪問
   - URL 中包含隨機 ID，不容易猜測

3. ⚠️ **建議**：
   - 不要公開分享 Apps Script URL
   - 在後端驗證請求數據
   - 考慮添加額外的認證機制（如果需要）

---

## 📝 完整檢查清單

完成以下步驟以解決 302 問題：

- [ ] 已前往 Google Apps Script 編輯器
- [ ] 已進入「部署」→「管理部署」
- [ ] 已確認「具有存取權的使用者」設置為「任何人」
- [ ] 已確認「執行身分」設置為「我」
- [ ] 已點擊「部署」按鈕重新部署
- [ ] 已等待部署完成
- [ ] 已清除瀏覽器快取或使用無痕模式
- [ ] 已重新訪問 Apps Script URL
- [ ] 已確認 Status Code 為 `200 OK`（不再是 302）
- [ ] 已確認可以直接看到 JSON 響應（沒有重定向）

---

## 🔗 相關文檔

- `BACKEND_CORS_FIX.md` - 完整的後端 CORS 處理指南
- `BACKEND_CORS_QUICK_FIX.md` - 快速修復指南
- `LOGIN_TROUBLESHOOTING.md` - 登入功能故障排除

---

## ✅ 總結

**302 Found 狀態碼 = 部署權限未設置為「任何人」**

**解決步驟**：
1. ✅ 設置「具有存取權的使用者」為「任何人」
2. ✅ 重新部署
3. ✅ 清除瀏覽器快取
4. ✅ 重新測試

**預期結果**：
- ✅ Status Code: `200 OK`
- ✅ 直接返回 JSON（沒有重定向）
- ✅ 包含 CORS headers

---

**最後更新**: 2025-12-16







